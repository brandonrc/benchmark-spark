# Python dependencies for benchmark-spark v2 orchestration and analysis.
#
# These are for the HOST side only (orchestration, analysis).
# TensorRT-LLM itself is NOT installed here — it lives inside the
# spark-single-gpu-dev container image, which is pulled by Docker.

# Core
numpy>=1.24.0
pyyaml>=6.0

# Statistics (bootstrap, TOST, Welch's t, Holm-Bonferroni)
scipy>=1.10.0

# Data wrangling (analysis convenience)
pandas>=2.0.0

# Optional: plotting for CI figures
matplotlib>=3.7.0
